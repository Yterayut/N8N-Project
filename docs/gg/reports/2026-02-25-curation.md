# GG Training Data Curation Report
**Date:** 2026-02-25
**Total cases analyzed:** 4

## Duplicates Found
*   **None identified.** While multiple cases exist for vendor **PTT (Tax ID: 107537000000)**, the ground truth values for common fields (e.g., `total: 1350`) are consistent across different request IDs, suggesting these are either unique receipts with the same amount or consistent labeling of the same document across different runs.

## Noise / Inconsistencies
*   **Case `tc_1772008956515_cov52h` (Vendor: unknown):** 
    *   **Lazy Labeling:** The `correct_value` for `customer_name` is entered as `...`. This is non-actionable for training.
    *   **Schema Mismatch:** Multiple fields (`address`, `currency`, `item_description`, etc.) are marked as `extra`. This suggests the OCR is capturing line-item detail that the current ground truth schema for "other" documents does not support or requires.
    *   **Vendor Identification:** The system has the Tax ID (107561000013) but the name is "unknown". This Tax ID actually belongs to **Internet Thailand Public Co., Ltd. (INET)** based on the OCR text.

## Low Quality Cases
*   **Case `tc_1772008956515_cov52h`:** Significant noise (as noted above) and lacks a proper vendor name despite a valid Tax ID. Accuracy is low (75%) but the corrections provided are insufficient to improve the model.
*   **Case `tc_1772007532315_6losj9`:** Missing `ocr_accuracy_pct` in the metadata, though the field diffs provide high-quality "missing field" ground truth.

## High-Quality Patterns (Keep These)
*   **PTT Fuel Receipt Corrections:** Cases `tc_1772007451073_yn04ia` and `tc_1772007532315_6losj9` provide excellent examples of **Amount Mismatches** and **Missing Field** recoveries for the most common vendor. These are high-signal for fine-tuning the `fuel` document model.
*   **Positive Samples:** Case `tc_1772007522296_6keh00` is a perfect extraction (100% accuracy) and should be kept to prevent model drift/regression.

## Recommended Actions
| case_id | action | reason |
|---------|--------|--------|
| `tc_1772007451073_yn04ia` | **KEEP** | High-quality correction for total amount mismatch. |
| `tc_1772007522296_6keh00` | **KEEP** | Validates 100% accuracy on standard PTT layout. |
| `tc_1772007532315_6losj9` | **KEEP** | Essential ground truth for recovering missing critical fields (Tax ID, Date, Total). |
| `tc_1772008956515_cov52h` | **FLAG/REMOVE** | Contains "..." as ground truth and inconsistent "extra" field flags. Needs manual re-labeling. |

## Summary for CC
The PTT (Fuel) dataset is performing well with consistent ground truth across manual and feedback sources. We have strong examples for fixing "amount mismatches" and "missing field" errors. However, **Case `tc_1772008956515_cov52h`** represents a failed curation attempt; the human labeler used ellipsis (`...`) instead of typing the actual customer name and flagged valid OCR data as "extra" rather than mapping it correctly. I recommend rejecting this specific case and updating our Tax ID master list to map `107561000013` to "INET" to improve auto-classification for "other" document types.
